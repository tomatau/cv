export enum Positions {
  lead = 'Lead',
  frontEnd = 'Front-end',
  backEnd = 'Back-end',
  fullStack = 'Full-stack',
}

export enum Verbosity {
  minimal = 'minimal',
  detailed = 'detailed',
}

export enum Options {
  recentExperienceOnly,
}
